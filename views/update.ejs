<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-800 text-white p-10">
      <div class="nav mb-10">
        <a href="/read" class="text-blue-600">Show Users</a>
      </div>
      <h1 class="text-3xl tracking-tighter mb-7">Update the Existing user!</h1>
      <form action="/update/<%=user._id %>" method="post">
        <input
          type="text"
          placeholder="Name"
          class="px-5 py-2 border-2 border-zinc-700 outline-none rounded-lg bg-transparent"
          name="name"
          value="<%=user.name %>"
        />
        <input
          type="text"
          placeholder="Email"
          class="px-5 py-2 border-2 border-zinc-700 outline-none rounded-lg bg-transparent ml-2"
          name="email"
          value="<%=user.email %>"
        />
        <input
          type="text"
          placeholder="Image URL"
          class="px-5 py-2 border-2 border-zinc-700 outline-none rounded-lg bg-transparent ml-2"
          name="imageURL"
          value="<%=user.imageURL %>"
        />
        <input
          type="submit"
          value="Update User"
          class="px-5 py-2 ml-3 bg-yellow-500 rounded-lg text-black"
        />
      </form>
    </div>
  </body>
</html>
